noinst_PROGRAMS = \
	test-os

test_os_CFLAGS = \
	-I$(top_srcdir) \
	$(OS_CFLAGS)
test_os_LDFLAGS = \
	$(top_builddir)/os/liboverlay-scrollbar-@OS_MAJORMINOR@.la

# FIXME(loicm): Figure out what tests must be compiles and add them here.
